PROMPT (copy/paste into Cursor / your UI generator / Figma AI):

Update my Terms Analyzer landing page so the large white empty space above the footer is removed and replaced with a green/teal CTA background that flows naturally into the navy footer, matching the style in my screenshots.

1) Goal (what must change)

There is currently a huge white blank area between the “How It Works” section and the footer (where only the Get Started Free button is floating).

Remove that empty white space completely by:

Making the CTA section taller (increase its min-height and vertical padding), and

Giving the CTA section a full-width teal/green background so it visually occupies that area, and

Ensuring the CTA background touches the footer directly with NO white gap.

2) Background + color requirements (must be followed)
CTA green background (this should cover the previous white space)

Use a rich teal-green that matches the overall aesthetic (not neon, not dull).

Use a vertical gradient for the CTA section:

Top: #0AA383

Middle: #089376

Bottom: #07806B

Add a subtle soft glow vignette (very light) so it feels premium, not flat.

Seamless blend into the footer (no visible break)

The bottom of the CTA must blend into the navy footer:

Blend band gradient (short transition area): #07806B → #07162E

Footer remains deep navy:

Primary: #081427

Secondary: #0A1B33

Zero spacing rule: No margin, no padding gap, no divider line, no “white strip” between CTA and footer.

3) CTA content layout (centered and visible)

Inside the green CTA block:

Headline (centered, large): “Ready to Stop Clicking ‘I Agree’ Blindly?”

Subtext: one short line under it.

Buttons row centered:

Primary button: “Get Started Free”

Secondary button: “View Pricing” (or “Learn More”)

The CTA must be tall enough so content sits comfortably in the middle, and the section still fills the previous whitespace.

4) Fix the “Install Extension” visibility issue

In the top navigation:

Replace the “Pricing” link with “Install Extension” (or “Download Extension”).

Ensure it’s visible and not hidden on desktop.

It should be a normal nav item like Features / How It Works / About (not a tiny button).

If you also have an Extension section in the footer links, keep it consistent.

5) Typography (must match my design choice)

Use San Francisco (SF Pro) system font:

SF Pro Display for headings (if available)

SF Pro Text for body (if available)

Fallback: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "Helvetica Neue", Arial, sans-serif

Keep the same bold, clean heading style from the screenshots (heavy weight for hero/CTA headings).

6) Spacing + section behavior rules

Do not leave any dead empty sections.

If the “How It Works” content ends early, the CTA section should start immediately after, with enough height to fill the screen naturally.

Keep the footer exactly once (no duplicates).

7) Final checks (must pass)

The area above the footer is NOT white anymore—it is covered by the teal/green CTA.

The CTA and footer touch with 0px gap.

“Install Extension” is visible in the navbar and replaces “Pricing”.

SF (San Francisco) typography is used across the page.

Make the changes pixel-clean, matching the premium SaaS look of my current layout